name: ArgoCD-Operations

bindings:

  - cluster: prod
    role: operator
  - cluster: dev
    role: operator

globalPermissions:
  - repositories:get
  - repositories:create
  - repositories:update
  - repositories:delete